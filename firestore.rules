rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 각 사용자는 자신의 데이터에만 접근 가능
    match /users/{userId}/{document=**} {
      allow read, write: if true;
    }
    match /activity_logs/{logId} {
      allow read, write: if true;
    }
    // 찬양팀 공유 채팅 및 신호 실시간 동기화
    match /teams/{document=**} {
      allow read, write: if true;
    }
  }
}
